import * as React from 'react';
import { Icon } from '@/Icon';
import { IconCommonProps } from '..';

export default function CloseOutline(props: IconCommonProps) {
  return <Icon iconName={'CloseOutline'} {...props} />;
}
