import * as React from 'react';
import { Icon, IconCommonProps } from '@/Icon';

export default function CopyOutline(props: IconCommonProps) {
  return <Icon iconName={'CopyOutline'} {...props} />;
}
