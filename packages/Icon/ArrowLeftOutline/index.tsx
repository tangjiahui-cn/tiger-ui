import * as React from 'react';
import { Icon, IconCommonProps } from '..';

export default function ArrowLeftOutline(props: IconCommonProps) {
  return <Icon iconName={'ArrowLeftOutline'} {...props} />;
}
