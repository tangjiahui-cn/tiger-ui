import * as React from 'react';
import { Icon, IconCommonProps } from '@/Icon';

export default function ArrowLeftDoubleOutline(props: IconCommonProps) {
  return <Icon iconName={'ArrowRightDoubleOutline'} {...props} />;
}
