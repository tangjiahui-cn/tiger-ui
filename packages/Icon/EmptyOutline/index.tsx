import * as React from 'react';
import { Icon, IconCommonProps } from '@/Icon';

export default function EmptyOutline(props: IconCommonProps) {
  return <Icon iconName={'EmptyOutline'} {...props} />;
}
