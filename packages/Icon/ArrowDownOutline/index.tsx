import * as React from 'react';
import { Icon, IconCommonProps } from '@/Icon';

export default function ArrowDownOutline(props: IconCommonProps) {
  return <Icon iconName={'ArrowDownOutline'} {...props} />;
}
