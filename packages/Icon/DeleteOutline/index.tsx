import * as React from 'react';
import { Icon, IconCommonProps } from '@/Icon';

export default function DeleteOutline(props: IconCommonProps) {
  return <Icon iconName={'DeleteOutline'} {...props} />;
}
