import * as React from 'react';
import { Icon, IconCommonProps } from '..';

export default function LoadingOutline(props: IconCommonProps) {
  return <Icon iconName={'LoadingOutline'} {...props} spin />;
}
