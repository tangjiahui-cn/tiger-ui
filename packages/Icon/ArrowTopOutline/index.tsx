import * as React from 'react';
import { Icon, IconCommonProps } from '@/Icon';

export default function ArrowTopOutline(props: IconCommonProps) {
  return <Icon iconName={'ArrowTopOutline'} {...props} />;
}
